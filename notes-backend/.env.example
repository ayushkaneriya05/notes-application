# MongoDB connection string
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/saas-notes?retryWrites=true&w=majority


# JWT secret (set a long random string in production)
JWT_SECRET=your_jwt_secret_here


# App port (Vercel will override; used for local dev)
PORT=4000


# Optional: seed flag (not required if you run seed script directly)
SEED_ON_START=false